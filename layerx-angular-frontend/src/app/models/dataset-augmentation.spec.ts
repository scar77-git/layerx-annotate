import { DatasetAugmentation } from './dataset-augmentation';

describe('DatasetAugmentation', () => {
  it('should create an instance', () => {
    expect(new DatasetAugmentation()).toBeTruthy();
  });
});
