import { TaskButtonStatus } from './task-button-status.model';

describe('TaskButtonStatus', () => {
  it('should create an instance', () => {
    expect(new TaskButtonStatus()).toBeTruthy();
  });
});
